# Sevalla Deployment Configuration
build:
  method: docker
  dockerfile: Dockerfile

# Environment variables
env:
  PORT: 8000
  DEBUG: 0
  PIP_FLAGS: "--pre"
  NIXPACKS_PIP_FLAGS: "--pre"

# Health check
health:
  path: /
  port: $PORT